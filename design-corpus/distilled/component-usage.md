# Component usage in the wild (distilled from the corpus)

DS components as actually used across the product: which variants appear, how often, and where the live product diverges from the component contracts in `packages/tokens/docs/components/`. Populated by `corpus/distill-corpus`. Cited by `ui/design-critique` (CMP) and component-contract maintenance.

Each observation carries `(corpus vN · <n> screens)` and links to manifest entries. The component contracts and `design-dna.md` component philosophy are the higher authority.

## Per component

_none yet — Button, Link, Chip, Label, Flag, Toast, Divider, Input, Image, and compositions once distilled_

## Divergence and misuse observed

_none yet_

## Gaps (patterns with no component yet)

_none yet_
