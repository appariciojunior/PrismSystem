# UI Kit Figma Publish Update

Added

- ✨ Added `xlarge` as a published IconButton size option.

Removed

- ⚠️ Removed Flag intent `channel`; active intents are `primary`, `secondary`, and `callout`.

Updated

- 📐 Button and IconButton now use the canonical size scale: `small=40`, `medium=48`, `large=56`, `xlarge=64`.
- 🎨 Divider tokens updated: horizontal dividers use `border/secondary`, vertical dividers use `border/tertiary`.

→ Questions or feedback: #ds-support ([Slack channel](https://newscorp.enterprise.slack.com/archives/C09ALD6M7CZ))
