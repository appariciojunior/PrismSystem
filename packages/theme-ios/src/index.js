// iOS Theme Package Entry Point
// This package exports iOS theme directories, each containing xcassets colors and Swift typography/spacing

export const iosThemes = {
  brand: './DSBrand/',
  core: './DSCore/',
  dataVisualisation: './DSDataVisualisation/',
  marketing: './DSMarketing/'
};

// Each theme directory contains:
// - Colors/ (xcassets)
// - Typography/
// - DSSpacing.swift
